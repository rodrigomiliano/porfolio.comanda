---
interface Props {
  title: string;
  detail: string;  
}

const { title, detail } = Astro.props;
---

<div
  class="relative mx-12 pb-8 before:absolute before:left-[-35px] before:block before:h-full before:border-l-2
  before:border-black/20 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative md:col-span-2">
    <div class="sticky top-0">
      <span class="text-purple-400 -left-[42px] absolute rounded-full text-3xl"
        >✔</span
      >

      <h3 class="text-xl font-bold text-purple-400">{title}</h3>
      <h4 class="font-semibold text-xl text-gray-600 dark:text-white">
        {detail}
      </h4>      
    </div>
  </div>
</div>
